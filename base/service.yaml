apiVersion: v1
kind: Service
metadata:
  name: tinysearch
  namespace: tinysearch
  labels:
    app: tinysearch
spec:
  selector:
    app: tinysearch
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
